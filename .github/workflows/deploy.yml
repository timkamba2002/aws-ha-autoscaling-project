name: CI/CD Pipeline - 3-Tier Web App

on:
  push:
    branches: 
      - development

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: Build React App
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
        working-directory: ./app
      - run: npm run build
        working-directory: ./app
      - uses: actions/upload-artifact@v4
        with:
          name: react-build
          path: app/build

  test-plan:
    name: Test & Terraform Plan
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::866934333672:role/GitHubActionsDeployRole-HAProjectV2
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init (Force Migration)
        run: terraform init -migrate-state -force-copy -no-color
        working-directory: ./terraform

      - name: Terraform Plan
        run: terraform plan -out=tfplan -no-color -lock=false -refresh=false
        working-directory: ./terraform

  deploy-development:
    name: Deploy to Development
    needs: [test-plan]
    if: github.ref == 'refs/heads/development'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: react-build
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::866934333672:role/GitHubActionsDeployRole-HAProjectV2
          aws-region: us-east-1
      - run: terraform apply -auto-approve tfplan
        working-directory: ./terraform